npm init
- go through dialogs ( press enter a bunch :D )

npm install -g browserify
- install globally

npm install --save babelify watchify babel-preset-react react react-dom
- install the rest of the needed packages